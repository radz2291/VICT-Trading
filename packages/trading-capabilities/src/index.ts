/**
 * Intentionally empty at T1. This package exists as a declared architectural
 * boundary for governed VICT capability boundaries arriving at Stage T2 —
 * no fake capabilities are created to populate it. See README.md.
 */
export {};
